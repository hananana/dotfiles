# ~/.tmuxinator/xbuild.yml

name: xbuild
root: ~/src/github.com/hananana/xbuild.vim

windows:
  - editor:
      layout: even-horizontal
      panes:
        - vim
  - git:
      layout: even-horizontal
      panes:
        - ls -l
