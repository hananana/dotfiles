# ~/.tmuxinator/rimuldar.yml

name: rimuldar
root: /mnt/hgfs/share/src/github.com/ClusterVR/Rimuldar

windows:
  - editor:
      layout: even-horizontal
      panes:
        - vim
  - git:
      layout: even-horizontal
      panes:
        - ls -l
