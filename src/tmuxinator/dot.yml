# ~/.tmuxinator/dot.yml

name: dotfiles
root: ~/.dotfiles

windows:
  - editor:
      layout: even-horizontal
      panes:
        - vim
  - git:
      layout: even-horizontal
      panes:
        - l
