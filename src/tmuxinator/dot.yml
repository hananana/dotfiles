# ~/.tmuxinator/dot.yml

name: dotfiles
root: ~/.dotfiles

windows:
  - editor:
      layout: even-horizontal
      panes:
        - nvim
  - git:
      layout: even-horizontal
      panes:
        - ls -l
