# ~/.tmuxinator/dot.yml

name: dotfiles
root: ~/src/github.com/hananana/dotfiles

# nvim + wslだとfold時にくっそ遅くなるのでvim
windows:
  - editor:
      layout: even-horizontal
      panes:
        - vim
  - git:
      layout: even-horizontal
      panes:
        - l
