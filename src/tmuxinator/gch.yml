# ~/.tmuxinator/gch.yml

name: gch
root: ~/src/github.com/hananana/gch

windows:
  - editor:
      layout: even-horizontal
      panes:
        - vim
  - git:
      layout: even-horizontal
      panes:
        - ls -l
