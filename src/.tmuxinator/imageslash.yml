# ~/.tmuxinator/imageslash.yml

name: imageslash
root: ~/Works/Projects/imageslash

windows:
  - editor:
      layout: main-vertical
      panes:
        - source ./venv/bin/activate && vim
  - git:
      layout: even-horizontal
      panes:
        - source ./venv/bin/activate
        - source ./venv/bin/activate && tig
