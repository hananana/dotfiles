# ~/.tmuxinator/til.yml

name: til
root: ~/Works/til

windows:
  - editor:
      layout: main-vertical
      panes:
        - vim
  - git:
      layout: even-horizontal
      panes:
        - ls -l
        - tig
