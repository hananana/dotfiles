# ~/.tmuxinator/schemes-selector.yml

name: schemes-selector
root: ~/Works/Projects/schemes-selector

windows:
  - editor:
      layout: main-vertical
      panes:
        - vim
  - git: 
      layout: even-horizontal
      panes:
        - ls -l
        - tig
