# ~/.tmuxinator/dot.yml

name: dotfiles
root: ~/.dotfiles

windows:
  - editor:
      layout: main-vertical
      panes:
        - vim -O .vimrc .zshrc
  - git:
      layout: even-horizontal
      panes:
        - ls -l
        - tig
