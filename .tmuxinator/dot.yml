# ~/.tmuxinator/dot.yml

name: dotfiles
root: ~/.dotfiles


windows:
  - editor:
      layout: main-vertical
      panes:
        - vim 
  - git:
      layout: even-horizontal
      panes:
        - tig
        - ls -l
