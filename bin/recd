#! /bin/bash
#
# filter underlying directories and stdout

err(){
    echo "[$(date +'%Y-%m-%dT%H:%M:%S%z')]: $@" >&2
}

if ! type peco >/dev/null 2>&1
then
    err 'must install peco. PLZ $brew install peco'
fi

find $(pwd) -type d -iname "*" | peco
